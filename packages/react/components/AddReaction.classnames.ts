import { cordifyClassname } from '../common/util';

export const addReaction = cordifyClassname('add-reaction');

export const addReactionClassnamesDocs = {
  [addReaction]: 'Applied to the add reaction button element.',
};
