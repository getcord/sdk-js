import { cordifyClassname } from '../../common/util.js';

export const emptyThreadPlaceholderContainer = cordifyClassname(
  'empty-thread-placeholder',
);
export const emptyThreadPlaceholderTitle = cordifyClassname(
  'empty-thread-placeholder-title',
);
export const emptyThreadPlaceholderBody = cordifyClassname(
  'empty-thread-placeholder-body',
);
